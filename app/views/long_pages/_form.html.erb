<%= form_for(@long_page) do |f| %>
  <% if @long_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@long_page.errors.count, "error") %> prohibited this long_page from being saved:</h2>

      <ul>
      <% @long_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
